apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: ClusterIP
  selector:
    app: nginx-app
  ports:
    - name: http
      protocol: TCP
      port: 8081      # porta exposta no Cluster
      targetPort: 80  # porta do NGINX dentro do pod
